import React from 'react';
import classes from './news.module.scss';

const News = () => {
    return (
        <div className={`${classes.news} ${classes.news__container}`}>
            News
        </div>
    );
}

export default News;